<%# For Home Page %>

<h1>
  <%= link_to gravatar_for(current_user, size: 52), current_user %>
  <%= current_user.username %>
</h1>
<h2>
  <span><%= link_to "view my profile", current_user %></span>
  <span><%= pluralize(current_user.microposts.count, "favorite thing") %></span>
</h2>
